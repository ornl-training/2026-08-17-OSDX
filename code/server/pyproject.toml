[project]
name = "server"
version = "0.1.0"
description = "FastAPI server for Web Development Tutorial"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pandas>=3.0.5",
]
